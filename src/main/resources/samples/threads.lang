// function definition

function hello(): void {
    print 1;
}

function add(x:int, y:int): int {
    return x+y;
}

thread1 := fork hello; // no parameters
thread2 := fork add (1, 2);
join thread1;
join thread2;